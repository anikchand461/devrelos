version: "3.9"

services:
  devrelos:
    build: .
    container_name: devrelos
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./devrelos.db:/app/devrelos.db  # persist SQLite DB
    restart: unless-stopped
    environment:
      - HOST=0.0.0.0
      - PORT=8000
